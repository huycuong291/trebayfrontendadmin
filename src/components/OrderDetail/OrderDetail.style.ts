import { createStyles } from '@mantine/core';

export const useOrderDetailStyles = createStyles((theme) => ({
  title: {
    fontSize: 30,
  },
  subTitle: {
    fontSize: 20,
    textDecoration: 'underline',
    fontWeight: 600,
  },
  textBold: {
    fontWeight: 600,
  },
}));
